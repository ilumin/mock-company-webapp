pipeline {
  /*
   *   See documentation: https://www.jenkins.io/doc/book/pipeline/syntax/#stages
   */
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'echo BUILD!'
      }
    }
    stage('Test') {
      steps {
        sh 'echo TEST!'
      }
    }
  }
}
